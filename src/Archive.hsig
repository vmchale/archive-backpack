signature Archive ( Entry
                  , packToFile
                  , unpackFromFile
                  ) where

data Entry

packToFile :: Foldable t => FilePath -> t Entry -> IO ()

unpackFromFile :: FilePath -> IO [Entry]
